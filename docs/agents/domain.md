# Domain Docs

Before exploring or changing RCA Orchestrator behavior, read the root `CONTEXT.md` and the relevant decisions under `docs/adr/`.

This is a single-context repository. Use the glossary's terms in issues, specs, test names, and implementation discussions. Surface an ADR conflict rather than silently overriding it.
