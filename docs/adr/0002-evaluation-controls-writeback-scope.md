# Let evaluation result control the writeback scope

Every completed RCA Run may offer a terminal-confirmed Jira status comment. Only a `passed` Evaluation Result may also attach the full RCA report; `needs_evidence` and `escalated` outcomes write a clearly labelled status comment so Jira records the follow-up without presenting an unsupported conclusion as a root cause.
