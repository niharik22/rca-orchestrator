# Use a local CLI as the RCA Orchestrator entry point

The MVP will be a local Python CLI, not a deployed RCA web service or skills-only workflow. The CLI provides a repeatable, testable execution path on the work laptop; skills guide its operation without duplicating RCA logic, and a UI can be added later above the same core.
